services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env          # copy .env.example to .env and fill in real values locally
    volumes:
      - ./database.db:/app/database.db   # keep SQLite data across container restarts
